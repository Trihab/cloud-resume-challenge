## Render Project Emulate Markdown

This backend is able to render markdown.
Our `render_project.py` will render our json with the markdown into html.
Eventually we'll rework this code into our serverless functions.